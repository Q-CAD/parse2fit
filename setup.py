from setuptools import setup

setup(
    name='parse2fit',
    version='0.1.0',
    author='Ryan Morelock',
    install_requires=[
        # List your package dependencies here
    ],
    scripts=[
    ],
    entry_points={
        'console_scripts': [
        ],
    },
    python_requires='>=3.9',
)

